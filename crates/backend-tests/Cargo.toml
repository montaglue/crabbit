[package]
name = "backend-tests"
description = "End-to-end tests that compile and run fixture crates through the crabbit backend dylib"
version = "0.0.0"
edition = "2024"
license.workspace = true
publish = false

[dev-dependencies]
pliron.workspace = true
pliron-ll.workspace = true
