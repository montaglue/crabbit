[package]
name = "crabbit-sudoku-solver"
version = "0.0.0"
edition = "2024"
publish = false

[[bin]]
name = "crabbit-sudoku-solver"
path = "src/main.rs"

# Keep this fixture out of the parent workspace. It is compiled explicitly with
# RUSTFLAGS pointing at the crabbit-rust codegen backend.
[workspace]
