[package]
name = "crabbit-device"
version = "0.1.0"
edition = "2024"
description = "VENDORED copy of kernel-corpus/crates/crabbit-device (keep link_names identical!): Minimal device-intrinsics shim for crabbit kernels: NVVM intrinsics as foreign declarations under their real LLVM names"

[lib]
crate-type = ["rlib"]
