[package]
name = "crabbit-backend-smoke"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
crabbit-device = { path = "../../../crates/crabbit-device" }
crabbit-kernel = { path = "../../../crates/crabbit-kernel" }

[[bin]]
name = "crabbit-backend-smoke"
path = "src/main.rs"

# Keep this fixture out of the parent workspace. It is compiled explicitly by
# the crabbit-rust backend smoke test/script with RUSTFLAGS pointing at the
# backend dylib.
[workspace]
