# Resume brief — kernel-corpus campaign, state at 2026-08-28 15:40

Everything is uncommitted in all repos (crabbit, eregalloc, combinatorial-
matrix-theory, cuda-oxide, kernel-corpus). Do not commit without the user.

## What is running unattended

- **Phase 5 clean sweep**: `scripts/perf-harness/harness.py --skip-backend-build
  --runs 20 --pin-cpu 7 --outdir scripts/perf-harness/results/20260831-clean-full`
  (detached with nohup; 8 configs from `scripts/perf-harness/configs.json`;
  backend snapshot `20260828-clean-full/libcrabbit.so`, sha256 df2122f6…,
  identical to `target/debug/libcrabbit.so` at launch). Order: 11 fixtures
  (done) → 14 corpus CPU variants (6/14 done at 15:33) → 4 arrow crates.
  Log: `20260828-clean-full/harness.log`; rows checkpoint into `results.csv`.
  Expected to finish ≈ 20:00 (arrow-array under C2 is the slow tail).
- **Post-processing watcher** `20260828-clean-full/postprocess.sh` (nohup):
  when the harness exits it runs `kernel-corpus/tools/cpu_analysis.py
  results.csv --out .` → `cpu_analysis.md` + `cpu_ratios.csv` in the same
  directory (`postprocess.log` records when).
- If the machine was rebooted or the harness died: re-run the same command
  with the SAME `--outdir` (it appends/checkpoints) or start fresh with a
  new timestamped dir. Never mix backend builds into one results dir.

## What is done

- Phase 1 (engines): `crates/crabbit/src/regalloc_engine.rs`,
  `pliron_ll::passes::aarch64::pipeline_with_allocator`, `targets.rs`
  hook, `conversion::pass::DynPass`. All fixtures correct under
  c0/c2 × uniform/spectral. Sibling fixes: eregalloc dialect attr keys
  renamed `spill_*` (dict-key collision with dialect-mir), eregalloc
  e-graph `Opq0` (arity-0 opaque). Harness gained engine axis, corpus
  targets, `--pin-cpu`, `--backend`, backend snapshotting, per-kernel-fn
  `kfn_*` metrics, compile-failure logs.
- Phase 2 (kernel→PTX): `emit_object` writes `<obj>.ptx`, `CRABBIT_PTX_OUT`,
  `CRABBIT_LL_OUT` (llvm-export). Fixture `kernel-vector-add`, test
  `crates/backend-tests/tests/kernels.rs` runs on the GB10. Conventions:
  `docs/KERNEL-ABI.md`.
- Phase 3 (corpus): `~/projects/montaglue/kernel-corpus` — 14 kernels ×
  4 artifacts, all ref.cu verified vs CPU checksums on the GPU.
- Phase 4 (GPU 3-arm): `kernel-corpus/tools/`; authoritative run
  `kernel-corpus/results/gpu/20260828-104400` (14×3×2 all correct).
- Backend bugs fixed today: i32/i16/i8 sdiv/srem sign-extension; ≥9
  integer args; FP math intrinsics. 180 workspace tests green; backend
  tests green under eregalloc.
- Analysis: `kernel-corpus/ANALYSIS.md` Part A (GPU) written.

## What remains (Phase 6)

1. When `20260831-clean-full/cpu_analysis.md` exists, write Part B of
   `kernel-corpus/ANALYSIS.md` from it, answering the brief's questions
   (1) eregalloc c0/c2 vs linear: executed spill traffic (`kfn_ldr_sp`,
   `kfn_str_sp`, `kfn_mov_imm` for the kernel fn; whole-object too) and
   runtime, per kernel and by family; (2) spectral vs uniform (three
   paired comparisons in the script); (5) which families discriminate.
   Early signals at 15:33 (medians, baseline → c0 / c2):
   elementwise_chain 5.31 → 7.10 / 7.02 s (eregalloc +34%);
   dot_product +8%; gemm_tiled 21.3 → 20.2 / 19.4 (−5…−9%);
   gemm_control, div_recurrence ties; compile time c2 +5…25%.
   Honesty rules from the brief: ties/losses as prominent as wins, flag
   single-kernel-driven results (script does), report compile-time cost.
2. Copy the CSVs the report cites into `kernel-corpus/results/cpu/`.
3. Caveat to state: corpus `cpu.repeat` was tuned for rustc-built binaries;
   crabbit-built ones are 3–57× slower (gemm_tiled 21 s/run), so runs are
   long but the medians are stable (min ≈ median).
4. Optional follow-ups surfaced (not started): translator state-space
   inference (`ld.shared`/`ld.global`) — cause of gemm_tiled's 125 regs;
   mid-end store-to-load forwarding / div-by-constant / LICM (seidel_2d);
   Rust-ABI aggregate returns via FnAbi (`DebugList` segfault); cuda-oxide
   `mir-transforms` pliron-0.17 port (unblocks route (i)).


## Session end 2026-08-31 ~14:05 (appended)

Everything below is uncommitted, all repos verified at their original heads.
Landed this session: TargetProfile + divergence-aware sink (GPU-verified);
backward-attribution v1 COMPLETE (machine + mid-end adjoints + server
run_costs + PTX linemap + ncu ingest + CRABBIT_MEASURED_COSTS); analysisd +
UI + e2e; real perf CPU profiles end-to-end; E1/E4 + GPU attribution
experiments done (see memory + results dirs). IN-FLIGHT WORK KILLED BY
SESSION END: the eregalloc-reliability agent (audit tool + eregalloc FP/
cost-model edits are ON DISK but UNVERIFIED — run every suite + corpus
checksums before trusting; then redo its decisive timed sweep; brief and
criterion recorded in memory "kernel-corpus-campaign"). Machine settings
persisted: perf_event_paranoid=1 (sysctl.d), nvidia profiler unrestricted
(modprobe.d). systemd-oomd still NOT installed (user chose it; one apt away).
